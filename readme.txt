To compile  run the following command:
$ make

To execute the subdivision program:
$ ./TextureSynthesis ../path_to/stripedsphere.obj

After running the program:
texture image and normal map are both saved to the same folder as the TextureSynthesis.cpp file.

